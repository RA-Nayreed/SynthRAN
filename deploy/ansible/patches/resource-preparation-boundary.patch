diff --git a/roles/pos/tasks/main.yml b/roles/pos/tasks/main.yml
--- a/roles/pos/tasks/main.yml
+++ b/roles/pos/tasks/main.yml
@@ -42,12 +42,6 @@
           echo "============================================================"
         } > .pos_debug_{{ node }}.log
 
-- name: (POS) Free any existing allocation for {{ node }}
-  shell: pos allocations free -k "{{ node }}"
-
-- name: (POS) Allocate {{ node }}
-  shell: pos allocations allocate "{{ node }}"
-
 - name: Load POS defaults
   include_vars: "{{ role_path }}/defaults/main.yml"
 
diff --git a/playbooks/deploy.yml b/playbooks/deploy.yml
--- a/playbooks/deploy.yml
+++ b/playbooks/deploy.yml
@@ -95,6 +95,7 @@
     - role: setup/pre_k8s
     - role: setup/k8s/k8s_setup
     - role: setup/k8s/k8s_env
+      when: not (synthran_prepare_only | default(false) | bool)
     - role: setup/optimization/cpu
     - role: setup/ovs
       #when: bridge_enabled
@@ -222,9 +223,9 @@
         file: "{{ playbook_dir }}/../group_vars/all/5g_profile_{{ fiveg_profile | default('default') }}.yaml"
   roles:
     - role: 5g/open5gs/config
-      when: core == 'open5gs'
+      when: core == 'open5gs' and not (synthran_prepare_only | default(false) | bool)
     - role: 5g/open5gs/deploy
-      when: core == 'open5gs'
+      when: core == 'open5gs' and not (synthran_prepare_only | default(false) | bool)
 
 - name: Deploy Free5gc
   hosts: core_node
@@ -281,12 +282,13 @@
         file: "{{ playbook_dir }}/../group_vars/all/5g_profile_{{ fiveg_profile | default('default') }}.yaml"
   roles:
     - role: 5g/srsRAN/config
-      when: ran == 'srsRAN'
+      when: ran == 'srsRAN' and not (synthran_prepare_only | default(false) | bool)
     - role: 5g/srsRAN/deploy
-      when: ran == 'srsRAN'
+      when: ran == 'srsRAN' and not (synthran_prepare_only | default(false) | bool)
     - role: 5g/srsRAN/csi
       when: 
         - ran == 'srsRAN'
         - csi_logger_enabled | default(false)
+        - not (synthran_prepare_only | default(false) | bool)
 
 - name: Deploy ueransim for oai core
   hosts: ran_node
