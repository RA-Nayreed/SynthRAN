diff --git a/roles/5g/open5gs/config/templates/amf-configmap.yaml.j2 b/roles/5g/open5gs/config/templates/amf-configmap.yaml.j2
index 37f2f1a72b1022a2e4925821adaed25b525514ba..4b3af8b42c46141a8b6c7897822fa36d527681c4 100644
--- a/roles/5g/open5gs/config/templates/amf-configmap.yaml.j2
+++ b/roles/5g/open5gs/config/templates/amf-configmap.yaml.j2
@@ -46,7 +46,7 @@ data:
             mcc: {{ fiveg.plmn.mcc }}
             mnc: {{ fiveg.plmn.mnc }}
           s_nssai:
-{% for slice in fiveg.slices %}
+{% for slice in fiveg.slices | selectattr('name', 'equalto', 'slice1') %}
             - sst: {{ slice.sst }}
               sd: {{ "FFFFFF" if slice.sd == "EMPTY" else slice.sd }}
 {% endfor %}
diff --git a/roles/5g/open5gs/config/templates/generate-data-fiveg.py.j2 b/roles/5g/open5gs/config/templates/generate-data-fiveg.py.j2
index 27d5e33498c0cb32e94b9a7024374e6c389fa516..43382d561e2b12a03c032eca201fc09527774791 100644
--- a/roles/5g/open5gs/config/templates/generate-data-fiveg.py.j2
+++ b/roles/5g/open5gs/config/templates/generate-data-fiveg.py.j2
@@ -23,7 +23,7 @@ DEFAULT_ARP = {"priority_level": 8,
 FIVEG_PROFILE = {
     "plmn": {"mcc": "{{ profile.plmn.mcc }}", "mnc": "{{ profile.plmn.mnc }}"},
     "slices": {
-    {% for s in profile.slices %}
+    {% for s in profile.slices | selectattr('name', 'equalto', 'slice1') %}
         "{{ s.name }}": {
             "sst": {{ s.sst | int }},
             "sd": "{{ 'FFFFFF' if s.sd == 'EMPTY' else s.sd.zfill(6) }}",
@@ -42,7 +42,7 @@ FIVEG_PROFILE = {
     {% endfor %}
     },
     "ues": {
-    {% for ue_name, ue in profile.ues.items() %}
+    {% for ue_name, ue in profile.ues.items() if ue_name == 'uesim01' %}
         "{{ ue_name }}": {
             "imsi_suffix": "{{ ue.imsi_suffix }}",
             "key": "{{ profile.security.full_key }}",
diff --git a/roles/5g/open5gs/config/templates/nssf-configmap.yaml.j2 b/roles/5g/open5gs/config/templates/nssf-configmap.yaml.j2
index c6d98c0fca319c198dfc6e0bee4cdb5ba9053bb7..131ad24cde03bb278eb5bbd23e82c1ba888fa03c 100644
--- a/roles/5g/open5gs/config/templates/nssf-configmap.yaml.j2
+++ b/roles/5g/open5gs/config/templates/nssf-configmap.yaml.j2
@@ -23,9 +23,9 @@ data:
           scp:
             - uri: http://scp-nscp:80
           nsi:
-{% for slice in fiveg.slices %}
+{% for slice in fiveg.slices | selectattr('name', 'equalto', 'slice1') %}
             - uri: http://nrf-nnrf:80
               s_nssai:
                 sst: {{ slice.sst }}
                 sd: {{ "FFFFFF" if slice.sd == "EMPTY" else slice.sd }}
-{% endfor %}
\ No newline at end of file
+{% endfor %}
diff --git a/roles/5g/open5gs/deploy/tasks/main.yml b/roles/5g/open5gs/deploy/tasks/main.yml
index bd9308d654bd7954182e68c5459a91faa187272c..cf23225069f8b06309ab8f0c225317c47fefa6ca 100644
--- a/roles/5g/open5gs/deploy/tasks/main.yml
+++ b/roles/5g/open5gs/deploy/tasks/main.yml
@@ -60,6 +60,7 @@
     systemctl restart kubelet
   become: true
   changed_when: false
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Restart CoreDNS pods after kubelet restart
   ansible.builtin.shell: |
@@ -67,6 +68,7 @@
   environment:
     KUBECONFIG: /etc/kubernetes/admin.conf
   changed_when: false
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Wait for CoreDNS to be Ready
   ansible.builtin.shell: |
@@ -80,6 +82,7 @@
   delay: 10
   until: wait_coredns.rc == 0
   changed_when: false
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Test CoreDNS resolution from core node
   become: true
@@ -94,6 +97,7 @@
     done
     kubectl delete pod dns-test-{{ groups['core_node'][0] }} --ignore-not-found
   changed_when: false
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Ensure Open5GS namespace exists
   kubernetes.core.k8s:
@@ -166,6 +170,7 @@
     state: present
     update_cache: yes
   become: yes
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Wait for Multus CRD
   ansible.builtin.shell: kubectl get crd network-attachment-definitions.k8s.cni.cncf.io
@@ -356,66 +361,37 @@
       - python3-wheel
     state: present
   become: yes
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Create Python virtual environment (idempotent)
   ansible.builtin.command:
     cmd: python3 -m venv "{{ repo_dest }}/venv"
     creates: "{{ repo_dest }}/venv/bin/activate"
   become: yes
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Bootstrap pip, setuptools and wheel in venv
   ansible.builtin.command:
     cmd: "{{ repo_dest }}/venv/bin/python -m ensurepip --upgrade"
   become: yes
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Upgrade pip, setuptools and wheel explicitly in venv
   ansible.builtin.command:
     cmd: "{{ repo_dest }}/venv/bin/pip install --upgrade pip setuptools wheel"
   become: yes
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Install Python requirements in venv
   ansible.builtin.command:
     cmd: "{{ repo_dest }}/venv/bin/pip install -r {{ repo_dest }}/requirements.txt"
   become: yes
-
-- name: Deploy Open5GS Web UI
-  ansible.builtin.command: kubectl apply -k "{{ repo_dest }}/open5gs-webui" -n {{ open5gs_ns }}
-
-- name: Wait for WebUI pod Ready
-  kubernetes.core.k8s_info:
-    kind: Pod
-    namespace: "{{ open5gs_ns }}"
-    label_selectors:
-      - "nf=webui"
-  register: webui_pod
-  until: >
-    webui_pod.resources | length > 0 and
-    (webui_pod.resources[0].status.containerStatuses[0].ready | default(false))
-  retries: 30
-  delay: 5
-  failed_when: false
-  changed_when: false
-
-- name: Warn if WebUI not ready
-  ansible.builtin.debug:
-    msg: "WebUI pod not Ready — continuing (optional component)"
-  when: webui_pod is failed or
-        webui_pod.resources | length == 0 or
-        not (webui_pod.resources[0].status.containerStatuses[0].ready | default(false))
+  when: not synthran_golden_path_guard | default(false) | bool
 
 - name: Run Python scripts to generate and add subscribers
   ansible.builtin.shell: |
-    {{ repo_dest }}/venv/bin/python {{ repo_dest }}/mongo-tools/generate-data-fiveg.py
-    {{ repo_dest }}/venv/bin/python {{ repo_dest }}/mongo-tools/add-subscribers.py
+    /opt/synthran-venv/bin/python {{ repo_dest }}/mongo-tools/generate-data-fiveg.py
+    /opt/synthran-venv/bin/python {{ repo_dest }}/mongo-tools/add-subscribers.py
   args:
     chdir: "{{ repo_dest }}"
-
-- name: Run add-admin-account.py
-  ansible.builtin.command: >
-    {{ repo_dest }}/venv/bin/python mongo-tools/add-admin-account.py
-  args:
-    chdir: "{{ repo_dest }}"
-
-- name: Check Open5GS deployment
-  ansible.builtin.include_role:
-    name: 5g/open5gs/check_all
\ No newline at end of file
+  no_log: true
diff --git a/roles/5g/srsRAN/config/tasks/apply_5g_profile.yaml b/roles/5g/srsRAN/config/tasks/apply_5g_profile.yaml
index 8373728f0f3909b3cacd0eea3f90a2054b5c09a6..268538fe23752ea10ef6e0f2efcc969b2aea0849 100644
--- a/roles/5g/srsRAN/config/tasks/apply_5g_profile.yaml
+++ b/roles/5g/srsRAN/config/tasks/apply_5g_profile.yaml
@@ -5,21 +5,10 @@
     path: "{{ yq_bin_path }}"
   register: yq_bin
 
-- name: Download yq binary using curl or wget
-  ansible.builtin.shell: |
-    set -e
-    if command -v curl >/dev/null 2>&1; then
-      curl -L --fail "{{ yq_url }}" -o "{{ yq_bin_path }}"
-    elif command -v wget >/dev/null 2>&1; then
-      wget -O "{{ yq_bin_path }}" "{{ yq_url }}"
-    else
-      echo "Neither curl nor wget found" >&2
-      exit 1
-    fi
-  args:
-    creates: "{{ yq_bin_path }}"
+- name: Refuse an unprepared yq dependency
+  ansible.builtin.fail:
+    msg: "the digest-locked yq binary required by SynthRAN is unavailable"
   when: not yq_bin.stat.exists
-  become: true
 
 - name: Ensure yq is executable
   ansible.builtin.file:
@@ -53,7 +42,7 @@
   set_fact:
     gnb_slices: "{{ (gnb_slices | default([])) + [ slice_entry ] }}"
     amf_slices: "{{ (amf_slices | default([])) + [ slice_entry ] }}"
-  loop: "{{ fiveg.slices }}"
+  loop: "{{ fiveg.slices | selectattr('name', 'equalto', 'slice1') | list }}"
   loop_control:
     loop_var: sl_item
   vars:
@@ -90,4 +79,4 @@
     block: |
       nodeSelector:
         kubernetes.io/hostname: "{{ ran_node_name }}"
-      nodeName: {{ ran_node_name }}
\ No newline at end of file
+      nodeName: {{ ran_node_name }}
diff --git a/roles/5g/srsRAN/config/tasks/main.yml b/roles/5g/srsRAN/config/tasks/main.yml
index 978431e6e4895d797110d3e89d0e09607f280873..a1ca5dbf81c7a3db05f463b42f641e4bd2aca20f 100644
--- a/roles/5g/srsRAN/config/tasks/main.yml
+++ b/roles/5g/srsRAN/config/tasks/main.yml
@@ -22,10 +22,12 @@
     file: "../../common/defaults/main.yml"
 
 - name: Ensure kubernetes python client is installed
-  ansible.builtin.package:
-    name: python3-kubernetes
-    state: present
-  become: yes
+  ansible.builtin.command:
+    argv:
+      - /opt/synthran-venv/bin/python
+      - -c
+      - import kubernetes
+  changed_when: false
   
 - name: Remove existing srsRAN Helm directory to ensure clean state
   ansible.builtin.file:
@@ -43,9 +45,7 @@
 - name: Ensure namespace exists
   kubernetes.core.k8s:
     api_version: v1
     kind: Namespace
     name: "{{ ran_ns }}"
     state: present
-  vars:
-    ansible_python_interpreter: "{{ ansible_playbook_python }}"
 
 - name: Gather env facts (remote)
diff --git a/roles/5g/srsRAN/deploy/tasks/deploy_gnb.yml b/roles/5g/srsRAN/deploy/tasks/deploy_gnb.yml
index 674bfa91925ddcecf5e651b285b910ce7c99dd5f..eaf78493a6b7d7a7044754fc506c92a1158ac9db 100644
--- a/roles/5g/srsRAN/deploy/tasks/deploy_gnb.yml
+++ b/roles/5g/srsRAN/deploy/tasks/deploy_gnb.yml
@@ -18,13 +18,10 @@
   changed_when: false
   failed_when: false
 
-- name: Install Helm if missing
-  ansible.builtin.shell: |
-    set -e
-    curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
+- name: Refuse an unprepared Helm dependency
+  ansible.builtin.fail:
+    msg: "Helm v3 must pass SynthRAN live preflight before deployment"
   when: helm_check.rc != 0
-  args:
-    executable: /bin/bash
 
 - name: Load common variables
   include_vars:
