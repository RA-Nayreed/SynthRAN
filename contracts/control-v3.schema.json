{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://synthran.invalid/contracts/control-v3.schema.json",
  "title": "SynthRAN control message v3",
  "oneOf": [
    {"$ref": "#/$defs/handshakeRequest"},
    {"$ref": "#/$defs/snapshotRequest"},
    {"$ref": "#/$defs/createExperimentRequest"},
    {"$ref": "#/$defs/resourcePreviewRequest"},
    {"$ref": "#/$defs/success"},
    {"$ref": "#/$defs/error"}
  ],
  "$defs": {
    "requestBase": {
      "type": "object",
      "required": ["v", "id", "method", "params"],
      "properties": {
        "v": {"const": 3},
        "id": {"type": "string", "minLength": 1, "maxLength": 128}
      }
    },
    "handshakeRequest": {
      "allOf": [
        {"$ref": "#/$defs/requestBase"},
        {
          "properties": {
            "method": {"const": "system.handshake"},
            "params": {"type": "object", "maxProperties": 0}
          },
          "required": ["method", "params"],
          "unevaluatedProperties": false
        }
      ]
    },
    "snapshotRequest": {
      "allOf": [
        {"$ref": "#/$defs/requestBase"},
        {
          "properties": {
            "method": {"const": "workspace.snapshot"},
            "params": {"type": "object", "maxProperties": 0}
          },
          "required": ["method", "params"],
          "unevaluatedProperties": false
        }
      ]
    },
    "createExperimentRequest": {
      "allOf": [
        {"$ref": "#/$defs/requestBase"},
        {
          "properties": {
            "method": {"const": "experiment.create"},
            "params": {
              "type": "object",
              "properties": {
                "intent": {
                  "enum": ["unspecified", "virtual-5g", "physical-5g", "open-ran", "iot-to-5g"]
                },
                "radio_mode": {"enum": ["automatic", "virtual", "physical"]},
                "label": {"type": ["string", "null"], "minLength": 1, "maxLength": 120}
              },
              "additionalProperties": false
            }
          },
          "required": ["method", "params"],
          "unevaluatedProperties": false
        }
      ]
    },
    "resourcePreviewRequest": {
      "allOf": [
        {"$ref": "#/$defs/requestBase"},
        {
          "properties": {
            "method": {"const": "resources.preview"},
            "params": {"type": "object", "maxProperties": 0}
          },
          "required": ["method", "params"],
          "unevaluatedProperties": false
        }
      ]
    },
    "success": {
      "type": "object",
      "required": ["v", "id", "ok", "result"],
      "properties": {
        "v": {"const": 3},
        "id": {"type": "string"},
        "ok": {"const": true},
        "result": {"type": "object"}
      },
      "additionalProperties": false
    },
    "error": {
      "type": "object",
      "required": ["v", "id", "ok", "error"],
      "properties": {
        "v": {"const": 3},
        "id": {"type": ["string", "null"]},
        "ok": {"const": false},
        "error": {
          "type": "object",
          "required": ["code", "message"],
          "properties": {
            "code": {"type": "string"},
            "message": {"type": "string"}
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  }
}
