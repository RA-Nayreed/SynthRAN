---
# vars/main.yml - derived, not overridable by group_vars

# Selected from deployment/topology.yml and included in the deployment hash.
ueransim_free5gc_network: "{{ synthran_topology.network }}"
ueransim_open5gs_network: "{{ synthran_topology.network }}"
ueransim_oai_network: "{{ synthran_topology.network }}"
nad_manifests_path: "{{ ueransim_chart_path }}/nad"
